# ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".

name: S3/Cloudfront Smart Invalidation -  save money on invalidations and maximize cache hits
description: I will analyze your changed files to S3 and minimize the number of Cloudfront invalidations while maximizing cache hits
runs:
  using: node16
  main: dist/index.mjs
branding:
  color: blue
  icon: refresh-cw
inputs:
  from-local-path:
    description: Path to sync the files from
    required: true
  to-s3-uri:
    description: No description
    required: true
  extra-arguments-s3:
    description: No description
    required: false
  distribution-id:
    description: No description
    required: false
  invalidation-strategy:
    description: |2-
       The balanced strategy will attempt to use the maximize-precision approach unless there are [limit] or more targets. 
      In that case it will switch to the minimize-invalidations strategy.
    required: false
    default: balanced
  balanced-limit:
    description: No description
    required: false
    default: 5
