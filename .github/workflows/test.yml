# ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".

name: release
on:
  push: {}
  workflow_dispatch: {}
jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    outputs:
      latest_commit: ${{ steps.git_remote.outputs.latest_commit }}
    env:
      CI: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Check if branch exists
        id: branch_exists
        run: git show-ref --quiet refs/heads/releases
        continue-on-error: true
      - name: Create a branch if necessary
        if: steps.branch_exists.outcome != 'success'
        run: git switch --orphan releases
      - run: ${{ steps.git_remote.outputs.latest_commit }} >> test.txt
      - uses: EndBug/add-and-commit@v9
        with:
          commit: --allow-empty
          push: origin releases --set-upstream --force
